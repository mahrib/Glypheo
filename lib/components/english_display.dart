import 'package:flutter/material.dart';

class EnglishDisplay extends StatelessWidget {
  const EnglishDisplay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(
      children: [
        Text("t"),
        Text("aye"),
        Text("p"),
      ],
    );
  }
}
